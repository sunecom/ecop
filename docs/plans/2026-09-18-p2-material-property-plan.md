# P2 组分与物性包实施计划

1. 固化实际库存、工具 schema、四组预探测结果和 NIST 参考点。
2. 新增受控 `material_flash` 后端模块、适配矩阵、严格回读与闭合校核。
3. 新增真实网页表单、结果、历史、比较与下载支持。
4. 新增契约、故障回放、HTTP 失败清理、API 正向/扰动/拒绝用例。
5. 使用独立 P2 DWSIM/Web、独立网络与数据卷构建固定候选。
6. 跑纯物质参考、二元双物性包、原九类回归及 1440/390 真浏览器验收。
7. 固化 commit、镜像、run_id、适用矩阵、证据哈希并交总控独立复核；不得部署生产或进入 P3。
