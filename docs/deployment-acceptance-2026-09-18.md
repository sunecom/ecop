# ECOP 云端受控演示部署验收记录

- 验收日期：2026-09-18（Asia/Shanghai）
- 正式地址：https://ecop.aitomoney.online
- 应用部署版本：`64e759b377ed0369b08e25df60e61afc82411ccd`
- Web 镜像 ID：`8e9ed8449a0a`
- DWSIM 镜像：`ghcr.io/danwbr/dwsim-mcp:10.2.8@sha256:015fd7ecbf219192d6b9d81a9080ba02fd0de7d4b9388506c4ab755159032f36`
- 验收范围：DWSIM 能力导航、真实组分/物性目录、三个纯水基础计算工作流、会话记录导出与同指标比较。

## 上线功能

1. 能力总览实时返回 48 个云端 MCP 工具、44 种单元操作、28 个物性包和 1611 条组分记录。
2. 设备库区分“已接入可运行”“引擎可用、网页待接入”“桌面或扩展能力”；只有完成专用参数和验收流程的 `Heater`、`Cooler`、`Pump` 标记为可运行。
3. `/api/compounds?q=` 在真实引擎目录缓存中搜索并只返回前 20 项；网页的物性包筛选使用引擎返回的准确名称。
4. 在线工作台提供目标汽化、按出口温度加热/冷却、泵升压三类实际 DWSIM 求解。
5. 每次求解都使用独立 flowsheet，并在 `finally` 中关闭；单引擎锁保持串行。只有目标值、相态和物料衡算通过后才返回成功。
6. 浏览器本轮会话记录支持逐条与整轮 JSON 导出，只允许比较同模块、同指标、同单位的两个工况；刷新页面即清空浏览器会话列表。

## 验收结果

| 检查项 | 结果 |
| --- | --- |
| Compose 配置 | `docker compose config --quiet` 通过 |
| 容器状态 | `ecop-web-1` healthy；`ecop-dwsim-1` 持续运行且部署时未重启 |
| 云端目录 | 48 工具、44 设备、28 物性包、1611 组分记录、3 个在线工作流 |
| 回环冒烟 | `python3 deploy/smoke_test.py` 全部通过，包含六次真实计算 |
| 公网 HTTPS | 公网冒烟完整执行六次真实计算以及 401/400/403 校验 |
| 浏览器验收 | Chrome 访问正式地址，Basic Auth、目录、温控计算、会话记录均通过，控制台无错误 |
| 请求防护 | 业务路由未认证返回 401；非法输入返回 400；错误 Origin 或 nonce 返回 403 |
| 接口边界 | 只暴露固定目录/搜索/计算接口，无任意 MCP 工具代理 |
| 端口隔离 | Web 仅发布 `127.0.0.1:18765`；DWSIM 无宿主端口 |
| 持久化 | 部署前 5 条记录；两轮六工况冒烟和线上浏览器计算后 18 条，原记录未丢失 |
| 回滚 | 文件包 `.rollback/pre-64e759b-20260918-132830.tgz` 与部署前 Web 镜像标签已保留 |

## 云端真实算例

| 模块 | 基准工况 | 基准结果 | 扰动工况 | 扰动结果 |
| --- | --- | --- | --- | --- |
| 目标汽化 | 1000 kg/h，25°C，101.325 kPa(a)，30% | 275.284347 kW；300 kg/h 汽相 | 汽化比例 60% | 463.329409 kW；600 kg/h 汽相 |
| 出口温度 | 1000 kg/h，25→60°C，101.325 kPa(a) | 净吸热 40.637067 kW | 60→25°C | 净吸热 -40.637067 kW（移热） |
| 泵升压 | 1000 kg/h，101.325→500 kPa(a)，效率 75% | 轴功率 0.148095 kW | 出口 900 kPa(a) | 轴功率 0.296681 kW |

六个工况的总流量残差均为 0.0 kg/h。目标温度、目标压力和汽相比例均达到输入规格；泵工况保持单液相，反算效率约 75%。旧版不含 `module` 字段的纯水汽化请求仍默认进入 `evaporation`，30% 基准值保持不变。

## 边界

在线计算仅验证纯水与页面给出的温压范围，统一采用 Steam Tables (IAPWS-IF97)。1611 条组分可搜索不代表全部组分都已开放计算。板式蒸发器选型、盐溶液浓缩、多效蒸发、MVR、机械设计、材料选择与安全审查仍不在本版本范围内。

结果记录中的 `commit` 字段标识固定 DWSIM 镜像摘要，不将其与本机源码 pin 或应用 Git 提交混淆。
